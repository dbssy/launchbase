{% extends "layout.njk" %}

{% block content %}

  <div class="card">
    <section style="background: url({{ instructor.avatar_url }}) no-repeat center center / cover"></section>
    <section class="details">
      <h3>Edit {{ instructor.name }}</h3>

      <form method="POST" action="/instructors?_method=PUT">
      {% include "instructors/fields.njk" %}
        <input type="hidden" name="id" value="{{ instructor.id }}">
      </form>

      <form id="form-delete" method="POST" action="/instructors?_method=DELETE">
        <input type="hidden" name="id" value="{{ instructor.id }}">
        <button type="submit">Deletar</button>
      </form>
    </section>
  </div>

  {% include "confirm.njk" %}

{% endblock %}